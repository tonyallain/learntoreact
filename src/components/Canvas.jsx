import React from 'react';
import Player from './player';

const Canvas = () => {
    return (
        <div className="canvas">
            <Player />
        </div>
    );
};

export default Canvas;
